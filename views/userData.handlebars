<nav class="navbar navbar-light bg-light" id="navbar">
  <a class="navbar-brand" id="homeButton" href="/userData">| Home |</a>
  <form class="form-inline">
    <input class="form-control mr-sm-2" type="search" placeholder="Find a Trail" id="search" aria-label="Search">
    <button class="btn btn-outline-success my-2 my-sm-0" type="submit" id="submitButton" href="/example">Search</button>
    <div class="g-signin2" data-onsuccess="onSignIn" data-theme="dark"></div>
  </form>
</nav>

<div class="container" id="resultsContainer">
  <div class="container" <div class="row">
    <div class="col-sm">
      Plan to Watch:
      <ul>
        {{#each movies.plan}}
        <li>
          {{this.title}} 
        </li>
        {{/each}}

      </ul>
    </div>
  </div>
</div>

<div class="container" id="resultsContainer">
  <div class="container">
    <div class="row">
      <div class="col-sm">
        Completed:
        <ul>
          {{#each movies.completed}}
          <li>
            {{this.title}}
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<div class="container" id="resultsContainer">
  <div class="container">
    <div class="row">
      <div class="col-sm">
        Dropped:
        <ul>
          {{#each movies.dropped}}
          <li>
            {{this.title}}
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>
